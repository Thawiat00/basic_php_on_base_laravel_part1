<?php
$ip = "127.0.0.1";

if (!filter_var($ip, FILTER_VALIDATE_IP) === false) {
  echo("$ip is a valid IP address");
} else {
  echo("$ip is not a valid IP address");
}
?>


<?php
echo '<br> <br>';

echo <<<EOT

Validate an IP Address <br>
The following example uses the filter_var() function to check if the variable $ip is a valid IP address: <br>



EOT;

echo '<br> <br>';

echo <<<EOT

โค้ดตัวอย่างที่คุณส่งมา เป็น ตัวอย่างการตรวจสอบว่า ค่าใดค่าหนึ่งเป็น IP Address ที่ถูกต้องหรือไม่ โดยใช้ฟังก์ชัน filter_var() ของ PHP ครับ <br>

มาอธิบายทีละส่วน: <br>

1. ตัวแปร $ip <br>
$ip = "127.0.0.1"; <br>


นี่คือ IP Address ที่เราต้องการตรวจสอบ ตัวอย่างนี้ใช้ IP แบบ IPv4 (127.0.0.1 คือ localhost) <br>

2. ฟังก์ชัน filter_var() <br>
filter_var($ip, FILTER_VALIDATE_IP) <br>


ฟังก์ชัน filter_var() ใช้ ตรวจสอบหรือทำความสะอาดข้อมูล <br>

FILTER_VALIDATE_IP คือ ตัวกรอง (filter) ที่ใช้ตรวจสอบว่าเป็น IP Address ที่ถูกต้อง <br>

ถ้า $ip เป็น IP Address ถูกต้อง จะคืนค่า ตัวแปรนั้นเอง <br>

ถ้าไม่ถูกต้อง จะคืนค่า false <br>

3. การตรวจสอบเงื่อนไข <br> 
if (!filter_var($ip, FILTER_VALIDATE_IP) === false) { <br>
  echo("$ip is a valid IP address"); <br>
} else { <br>
  echo("$ip is not a valid IP address"); <br>
}


filter_var($ip, FILTER_VALIDATE_IP) จะคืนค่า $ip ถ้าถูกต้อง <br>

! ... === false ใช้เพื่อ หลีกเลี่ยงปัญหาเวลา IP เป็น "0.0.0.0" เพราะถ้าใช้แค่ if(filter_var(...)) จะถือว่า false ซึ่งไม่ถูกต้อง <br>

ถ้าเงื่อนไขผ่าน จะแสดงข้อความว่า "127.0.0.1 is a valid IP address" <br>

ถ้าไม่ผ่าน จะแสดง "xxx is not a valid IP address" <br>

🔹 สรุป <br>

โค้ดนี้ ตรวจสอบว่า $ip เป็น IP Address ที่ถูกต้องหรือไม่ <br>
สามารถใช้ได้ทั้ง IPv4 และ IPv6 โดย FILTER_VALIDATE_IP รองรับทั้งสองแบบ <br>


EOT;


?>