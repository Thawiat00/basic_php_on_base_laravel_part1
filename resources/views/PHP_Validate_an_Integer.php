<?php
$int = 100;

if (!filter_var($int, FILTER_VALIDATE_INT) === false) {
  echo("Integer is valid");
} else {
  echo("Integer is not valid");
}
?>


<?php

echo <<<EOT

Validate an Integer
The following example uses the filter_var() function to check if the variable $int is an integer. 
If $int is an integer, 
the output of the code below will be: "Integer is valid". 
If $int is not an integer, the output will be: "Integer is not valid":


EOT;

echo  '<br> <br>';


?>


<?php

echo <<<EOT

อธิบายให้เข้าใจง่าย ๆ เลยนะ 😊

โค้ดตัวอย่างนี้ใช้ filter_var() ของ PHP เพื่อ ตรวจสอบว่าตัวแปร $int เป็นจำนวนเต็ม (integer) หรือไม่

รายละเอียดทีละบรรทัด
$int = 100;


กำหนดค่าตัวแปร $int เป็น 100

if (!filter_var($int, FILTER_VALIDATE_INT) === false) {
  echo("Integer is valid");
} else {
  echo("Integer is not valid");
}


filter_var($int, FILTER_VALIDATE_INT)

เป็นฟังก์ชันของ PHP ที่เอาไว้ ตรวจสอบค่าของตัวแปร

ตัวเลือก FILTER_VALIDATE_INT บอกให้ตรวจสอบว่าตัวแปรเป็น จำนวนเต็มหรือไม่

ถ้า $int เป็นจำนวนเต็ม จะ คืนค่า $int กลับมา

ถ้า $int ไม่ใช่จำนวนเต็ม จะ คืนค่า false

!filter_var(...) === false

มาตรฐานในตัวอย่างนี้เป็นการเช็คว่า filter_var() ไม่คืนค่า false

ถ้าไม่คืนค่า false หมายความว่าเป็น integer → แสดง "Integer is valid"

ถ้าคืนค่า false → แสดง "Integer is not valid"

สรุป

$int = 100 → เป็นจำนวนเต็ม → จะเห็นข้อความ:

Integer is valid


ถ้า $int = "hello" หรือ $int = 10.5 → จะไม่ใช่ integer → จะเห็นข้อความ:

Integer is not valid


💡 Tip: ฟังก์ชัน filter_var() ใช้ตรวจสอบข้อมูลชนิดต่าง ๆ ได้มาก เช่น email, URL, boolean, float ฯลฯ และยังสามารถทำ sanitize ข้อมูลได้ด้วย


EOT;

echo  '<br> <br>';


?>
