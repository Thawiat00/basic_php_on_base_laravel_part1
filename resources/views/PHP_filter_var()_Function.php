<?php
$str = "<h1>Hello World!</h1>";
$newstr = filter_var($str, FILTER_SANITIZE_STRING);
echo $newstr ;
echo  '<br> <br>';
?>


<?php
echo <<<EOT

PHP filter_var() Function
The filter_var() function both validate and sanitize data.

The filter_var() function filters a single variable with a specified filter. It takes two pieces of data:

The variable you want to check
The type of check to use
Sanitize a String
The following example uses the filter_var() function to remove all HTML tags from a string:

EOT;
echo  '<br> <br>';
?>

<?php
echo <<<EOT
filter_var() เป็นฟังก์ชันใน PHP ที่ใช้ ตรวจสอบ (validate) และ ทำความสะอาด (sanitize) ข้อมูล

ตรวจสอบ (validate): ตรวจสอบว่าค่าที่ส่งมาถูกต้องตามเงื่อนไขหรือประเภทที่กำหนดหรือไม่ เช่น ตรวจสอบว่าเป็นอีเมลจริงหรือไม่

ทำความสะอาด (sanitize): ลบหรือแก้ไขอักขระที่ไม่ต้องการออกจากข้อมูล เพื่อให้ปลอดภัยต่อการใช้งาน เช่น ลบ HTML tags หรือสัญลักษณ์พิเศษ

โครงสร้างของฟังก์ชัน

filter_var(ตัวแปร, ประเภทของการตรวจสอบ/ทำความสะอาด);

ตัวแปร → ข้อมูลที่เราต้องการตรวจสอบหรือทำความสะอาด

ประเภทของการตรวจสอบ/ทำความสะอาด → กำหนดชนิดของ filter เช่น FILTER_SANITIZE_STRING, FILTER_VALIDATE_EMAIL เป็นต้น

ตัวอย่างในโค้ดที่ให้มา

<?php
\$str = "<h1>Hello World!</h1>";
\$newstr = filter_var(\$str, FILTER_SANITIZE_STRING);
echo \$newstr;
?>

เรามีตัวแปร \$str ซึ่งเก็บค่าเป็น "<h1>Hello World!</h1>"

เราใช้ filter_var() กับ filter ชนิด FILTER_SANITIZE_STRING

filter นี้จะ ลบ HTML tags ออกจากสตริง

ผลลัพธ์ที่ได้เก็บไว้ใน \$newstr → "Hello World!"

พอ echo \$newstr; ก็จะแสดงข้อความ Hello World! โดยไม่แสดง <h1>

💡 สรุปง่าย ๆ:
filter_var() ช่วยให้เรา ทำให้ข้อมูลปลอดภัย หรือ ตรวจสอบความถูกต้อง ก่อนนำไปใช้ โดยในตัวอย่างนี้เป็นการ ลบ HTML tags ออกจากข้อความ
EOT;
?>